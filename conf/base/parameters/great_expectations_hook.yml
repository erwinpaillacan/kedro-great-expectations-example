great_expectations:
  context_root_dir: #"conf/base/great_expectations"
  checkpoint_mapper:
    # datasename : #checkpoint (notice that a suite needs to be linked to a checkpoint)
    "preprocessed_companies": "companies.intermediate"
